package ru.maxthetomas.e418.codecs;

/**
 * Interface required for any condition and event that could request a number, to use `this` when using get(context, this).
 */
public interface NumberRequester {
}
